# Jekyll local build/test toolchain (see Gemfile, tests/)
_site/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/

# Node test-only deps (see package.json, tests/test_mathjax.cjs)
node_modules/

# Lean/lake build artifacts (verify/ lake project; see id:3317)
.lake/

# git-diary-workflow parallel-session lock
.git-lock-push.lock

# diary workflow temp files
.diary-entry.*

# owner-private notes — never publish (personal journals etc.); see private/
private/

# Python bytecode (dreamed search scripts)
__pycache__/
*.pyc

# flock files for the shared non-union ledgers (md-merge.py / commit-ledger.sh)
TODO.md.lock
ROADMAP.md.lock
REVIEW_ME.md.lock
