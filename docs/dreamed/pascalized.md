---
title: "Pascalized: the dreamed tree, distilled"
permalink: /dreamed/pascalized
---

# Pascalized: the dreamed tree, distilled

> **DREAMED, UNREVIEWED.** See [`README.md`](README.md). The 58 essays, each reduced to its claim
> (not its topic) by repeated distillation until the next cut would remove the claim rather than its
> packaging (`TODO.md id:a1aa`, Pascal, Letter XVI). Where a claim did not survive later checking,
> the same sentence says so. Figures, posters and reviews are out of scope.

## The tree in one paragraph

Three sessions, 58 essays, one question put to the owner's own ideas: pushed to a theorem, what
survives? The answer is uniform and was never stated as a thesis: **every seed that survived did so
by shrinking to a smaller, sharper object than the one it was named after.** The Bloch ball became a
report triangle whose third coordinate is gauge (ratified D1/D2); WiRoHSH became Whittaker 1903 plus
a NO-GO; "encrypted algorithm" became three problems sorted by who holds the key; the FHE-LLM wall
became an invoice; the five-level laser became the second law in rate-equation clothes; omniscience
fell to arity; narrativium inverted its own myth. The tree established boundaries, not physics:
eleven owner-authored lines located as wrong or unstated, and a working mode of self-refutation
(three of four speculative essays killed their own construction, four retractions across six FHE
essays, 5 of 37 audited citations wrong). Its one measurement, the scheduler prototype, found the
ratified fork has less content than the eight essays arguing it assumed.

## Method, and five ladders

Each claim was cut three or four times from the previous cut, a round kept only if briefer **and**
better (a dropped clause was hiding a vagueness), discarded if merely shorter. Most fixed after two
rounds; those still improving at three are shown, since their essays never found their point.

**lasercool §4.** (1) "The threshold at which stimulated emission starts to dominate and the threshold at which the exhaust stops carrying k_B-scale entropy per photon are the same threshold." (2) "Stimulated emission dominates exactly when the exhaust stops carrying entropy." (3) "A laser cannot be the exhaust: the brightness that makes it a laser is what empties it of entropy." A fourth cut ("the exhaust must be dim") drops the mechanism.

**logic-layered-core §4.4, with counterfactual-boundary §6.4.** (1) "The Kernel's inability to express the Theory's provability predicate is not a limitation to be engineered around. It is the safety property." (2) "The core is safe because it cannot say what it supervises." (3) "A core that cannot name a proof is a dashboard, not a supervisor; its safety is bought by demotion." Round 2 exposed "supervises" as the vagueness; the sibling had shown the core supervises nothing.

**zkml-proofs §1.** (1) "A zkML proof over private weights certifies an existential, some admissible weights produce this output, not that the promised model ran, so it answers the seed only if zero-knowledge over the weights is dropped, which is the property the provider wanted." (2) "A proof over secret weights proves a model of the declared shape ran, not the promised one." (3) "You can prove you ran your model only by publishing it."

**logic-models-vs-epistemic §2.3.** (1) "Under (i), z = 0 forces r = 0, so 'I proved this is undecidable here, stop asking' and 'I have got nowhere yet, spend more budget' are the same point." (2) "A state over models cannot tell 'proved undecidable' from 'not yet tried'." (3) "A theory determines independence; only a search has ignorance."

**wirohsh-discontinuities §7.1.** (1) "Smoothness is not the criterion; real-analyticity is. A C-infinity bump with compact support is infinitely smooth and is not the restriction of any holomorphic function anywhere on the boundary of its support." (2) "The line is analytic versus not, and a smooth bump is on the shock's side." Stopped at 2: "a bump and a shock are the same to a Laurent series" is false, since the essay measured different decay rates. Contrast omniscience, whose essay reached its fixed point unaided.

## The compressed tree

### Bloch Truth, first wave (2026-09-07)

- **weltformel-impossibility.** The Faizal papers establish only that a theory interpreting arithmetic cannot certify itself (Tarski, which survives non-r.e. axioms where Goedel I does not): a limit on self-reference, not physics. Their Chaitin claim is false ("r = r" is provable); "an inconsistent universe" is a category error.
- **logic-bloch-poles.** Three statuses cannot inject into two poles, so poles are provable/not; true/unprovable is refuted since Goedel's sentence is both. The ball is Kleene, not Belnap. Superseded: mixed states retract the every-status-at-a-pole premise; D2 replaced the thesis.
- **logic-bloch-gates.** The angle is no truth value: a circle has no rotation-covariant order. CNOT gives r' = sqrt(1 - C^2), kickback is its b_x = -1 corner, and H tensor H reverses control and target. Three wires for AND is a theorem.
- **logic-qutrit-su3.** det rho >= 0 makes the qutrit body not a ball, |n3| + p_undec <= 1, and a pure value's antipode is not a value, so no distinguished negation. Deflated later: the inequality is p >= 0 on a simplex, not an SU(3) fact.

### Bloch Truth, second wave

- **logic-models-ensemble.** A state over models is a measure on Cantor space; every such state is diagonal, so the ball collapses to |z| = r and a simplex is forced. Fatal, self-reported: theorems get probability 1, so (i) has independence, never ignorance.
- **logic-epistemic-state.** Four statuses put the corners at poles, circle and origin with |z| <= r derived. Against itself: (0, 1) also holds Ellsberg's fair coin, and no quantum structure is needed. Its Josang identification was wrong (u = ind + open).
- **logic-models-vs-epistemic.** Build on (ii) in the triangle {|z| <= r <= 1}, the ball's exact (z, r) shadow, and re-type (i) as soundness semantics. The encoding-(b) convergence is not real; the simplex convergence is one theorem four times. Ratified D1/D2.
- **logic-bloch-phase.** Provability cannot be the phase: box distributes over idempotent conjunction and an idempotent group element is 1 (K unused, so no modality repairs it); GL frames have no cycles. Proof-relevance wins but yields Z, blind to the roots of unity. Its "look for Z2" was refuted by its child.
- **logic-beyond-su3.** Equal-trace density matrices are Loewner-incomparable, so the Bloch body is an antichain: values, no connectives. Ranked: Heyting plus Kripke, GL beside as the decidable metalayer. Corrected: four vertices cost three reals, a Bloch vector's price.
- **logic-simplex.** The conflation is forced by mixing alone (the ball's centre is the even mixture of its poles). Connectives exist but indep AND indep = open, so no lattice; the three-status fragment is K3 relabelled and the novelty is one cell. Learning is an irreversible monoid, so the gates are lost at no cost.
- **logic-z2-grading.** A group-valued invariant multiplicative over an idempotent operation is trivial, so Z2 dies as U(1) did; a Boolean ring has no odd part. Survivor: the sign of a proof's atom-occurrence permutation. Its BPI convergence later dissolved.
- **logic-layered-core.** A quantifier-free report calculus is complete because too weak to describe itself. A core certificate is never a shortcut (Loeb), exact reports would decide theoremhood, and no truth-functional conjunction on reports exists. Not new: proof-carrying code, Milawa, partial reflection.
- **citation-audit.** 28 of 37 confirmed, 5 wrong. Sperling and Walmsley 2018 publish the geometry (true/false poles, undecidable equator, double cone); the logic is not published. That sentence became D2. A silent abstract plus an unreadable PDF is evidence of nothing.

### Bloch Truth, speculative wave

- **logic-complementarity.** No pair of questions about a sentence is complementary: two functions of one point always have a joint, the meet. "Name a pair" and "justify the ball" are one demand. Needing no pair: proofs can be copied, and non-simplex states cannot.
- **logic-counterfactual-boundary.** Addition and multiplication are each harmless (Presburger, Skolem); the pair is fatal, through pairing. Addition plus the bare set of squares already defines multiplication: the cliff is a set, not an operation. A core that cannot interpret arithmetic cannot represent a proof: a dashboard, not a supervisor.
- **logic-proof-gauge.** A discrete connection is flat iff exact, and confluent normalisation supplies the potential, so the gauge theory of proofs is exactly flat: the sibling's sign is a potential, not a holonomy (the braiding is a second vertex). Non-confluent classical cut collapses all proofs (Lafont).
- **logic-thermodynamics.** A partition function over proofs exists iff beta > log a (Hagedorn), but a is a constant of the proof system, equal for one theorem and for all, so the temperature has no goal. r is not an order parameter; z is what sharpens at the SAT threshold. Landauer is 10^11 to 10^22 from binding.
- **logic-bpi.** BPI is real, weaker than AC, and about the wrong object: a countable Boolean algebra has a prime ideal in ZF by enumeration, and every countable-language Lindenbaum algebra is countable. The live axis is computability (WKL_0), not choice.
- **logic-scheduler-prototype.** The two readings agree to every digit until an independence proof is bought, and at any plausible price the purchase loses (break-even a third to three quarters of an ordinary proof). The (0, 1) conflation costs a third of throughput; (i) is immune. The interface admits removal, never prioritisation.

### FHE cluster (2026-09-04, fhe-counting from 09-01)

- **fhe-toy-enumeration.** A strict two-operation scheme keeps log2 k key bits on k bits against the OTP's k; key entropy and functional completeness are exactly incompatible; randomising buys zero ambiguity. Whatever makes FHE hard is not algebra.
- **fhe-encrypted-algorithm.** "Encrypted algorithm" is three problems sorted by who holds the key: private function evaluation (solved), obfuscation (VBB impossible), circuit privacy (a cost). An encrypted program cannot branch on its data, so it is a circuit, and a general program has no worst case to pad to.
- **fhe-llm.** No theorem forbids an FHE-LLM; the gap is 10^3 to 10^4 in latency, 10^6 in bandwidth, and lives in the nonlinearities, not the matmuls. Corrected: no end-to-end pure-FHE generative datapoint exists; "zero integrity" retracted.
- **model-attestation.** Identity is a two-sample test, cheaper than correctness; an activation fingerprint carries 150 times a sampled token's evidence. Under FHE a provider cannot recognise an audit, so encryption makes sampled integrity checking unevadable.
- **zkml-proofs.** zkML overhead is 10^3.2 to 10^3.8 and sublinear in model size, so engineering. You can prove you ran your model only by publishing it. Commit-then-challenge makes sampled proving a 3.66x business at a 1000x bond.
- **trustless-distributed-ai.** Autoregressive decoding is predictive coding, so float drift compounds; codecs fixed the same problem with bit-exact integer transforms. Partly retracted: integer models are bit-exact, but their bit-shift nonlinearities are the hardest exact-FHE primitives.
- **fhe-counting.** Three counting debts closed with frozen signatures; the fourth (balanced iff semi-destructive) is an identification, so a proof would only fix a stipulation the owner must ratify. The Stirling finding is confirmed: a non-shrinking +0.4068-bit offset.

### WiRoHSH (2026-09-01)

- **wirohsh-splats.** The reduction closes because the Wick rotation makes the direction null, so any profile is harmonic: Whittaker 1903, the b family redundant. NO-GO on id:ff32: one-sided splats, no wave equation, wrong domain, wrong compression direction.
- **wirohsh-discontinuities.** Non-holomorphic data survives as a hyperfunction boundary value once the common annulus is dropped. The line is analytic versus not, and a smooth bump is on the shock's side, so wirohsh.md:82 is false as written.
- **wirohsh-refraction.** In 1+1 the conformal class survives any index (rays, Snell) but harmonicity dies to a drift; the gauge w = sqrt(n) f leaves a potential vanishing iff n is constant or A/(x - x0)^2. Fresnel is the two-phase Schwarz reflection principle.
- **wirohsh-ladder.** The ladder's parity is not Huygens: d = 1 is the ladder's best floor and Huygens' exception. Each rung hands back the continuous parameter the rotation was meant to remove; the payoff belongs to the Fourier relabelling.
- **wirohsh-approximation.** The missing cell in Fourier : transform :: Laurent : ? is the two-sided Laplace (Mellin) transform. A real-analytic function has no compact support, so the basis can never localise: a fifth NO-GO. Gibbs is 8.95 % in both bases.

### Spine and standing questions (2026-09-01)

- **spine.** Before l.126 the maths arrives before the question; after it the questions arrive and no maths does. l.79 calls complex numbers unorderable and l.89 writes an inner product > 0. l.105's equality case is real; over C it is the unit circle, which is rays.
- **time-and-operators.** Three questions the reader would ask force a one-parameter unitary group; Stone then derives H and Schroedinger, and without the i evolution is diffusion. Equation t1 can come from no U(t) unless p is 0 or 1.
- **measurement-routes.** POVM first, decoherence second (it needs the unpaid dynamics), reversing spine.md. POVMs contain the projective postulate as a theorem and generalise Born rather than derive it; Gleason excludes the coin; decoherence explains the basis, never the outcome.
- **omniscience.** Omniscience fails by arity, not capacity: a knower inside the known has shape A to (A to B), and Lawvere forbids point-surjection. Goedel I does not apply to the ket 42. Corrected: for a world formula Tarski outranks Lawvere, needing no effectivity.
- **q2-galilei-vs-poincare.** In 1+1 Galilei's boost-translation charge is a nontrivial class (mass) while Poincare's is a coboundary; dim H^2(Poincare) is 1 there, not 0. Galilei-first must teach cohomology at step 3, the corpus gap; recommends the on-ramp hybrid.
- **information-wing.** The wing coheres around log W, not "entropy". fhe.md contains no FHE, and FHE has no information-theoretic content; its OTP line is Shannon's H(K) >= H(M).
- **methodology-themes.** "Move the problem, solve, move back" has about eight owner instances; "reversibility is sacred" has one and no owner file contains "revers": a slogan. Missed: "the complex plane pays rent".
- **love-wing.** Two junctions are real, one thematic, one an epistemic gear change (Gottman is fitted; its 94 % is in-sample). The thread is a Lyapunov gain-loss scalar, not the order parameter, and it breaks at Gottman.
- **photon-localizability.** Newton-Wigner forbids a photon position operator and says nothing about how concentrated a classical A_mu may be. The Gaussian ansatz does not solve the free wave equation, and a per-index width is not covariant.
- **q6-q7-q8-apparatus.** Tier and status are orthogonal (25 states): extend the piloted kind family to five nouns. A cross-file eqref renders (???) silently, so Q8 must be decided before authoring.

### Thermodynamics, entropy, laser wing (2026-09-01)

- **lasercool.** An atom sheds at most 4 omega_rec/Gamma k_B per photon against 2 k_B per fluorescence photon: margin 10^3, not 10^7. A laser cannot be the exhaust: the brightness that makes it a laser empties it of entropy. Reabsorption is a rate problem.
- **five-level-laser.** The gain numerator's only positive term needs Q < 0, so inversion and cooling are exclusive at any pump strength and intensity: the second law as algebra. The dictated "relax 2 to 3" plus cooling is inconsistent; the step must run uphill.
- **photon-energy-scaling.** No COP-optimal photon energy exists (Carnot is the supremum at every frequency). A power optimum exists because A goes as nu^3, at 3.92 k_B T (Wien with exponent 4, 12 µm at 300 K). Only the k_B T B wall survives a cavity.
- **wick-entropy.** The Laurent and Matsubara indices are one basis under z = exp(w); reading the polar angle as the thermal circle forces Unruh: the owner compactified the wrong circle, the interesting one. N = 2 equals antiperiodic at two points only.
- **lambertw-statistics.** The bosonic inversion needs W_{-1}; every CAS default returns beta E_1 = 0 identically. The fermionic one always exists and is two-valued. The N family is Gentile statistics, not anyons.
- **acoustics.** acoustics.md never derives the wave equation and states its adiabatic assumption only through the notation c^2/gamma. Sound can bend without echoing; light cannot, since Z = Z_0/n ties reflection to refraction.

### Corpus items ratified for authoring (id:e552)

- **casimir-field-equations.** P^2 = m^2 is Klein-Gordon for every spin, sufficient only at s = 0; W^2 selects the irrep and never gives Dirac, a square root of P^2 via an odd worldline-SUSY generator. Row P-C misdescribes q/27195 (variational).
- **generators-and-bch.** Translation is Taylor and needs analyticity; dilation is diagonal on monomials (Mellin); the cross product is the so(3) bracket. For d/dt e^X both naive orderings and their average fail. Row M-1 mislabels 337971, which holds a live 13-year gap of the owner's.
- **discrete-noether.** A discrete symmetry gives no current but a multiplicative Z_n quantum number: selection rules and degeneracies, charges multiplying where Noether's add. Bloch quasi-momentum modulo 2 pi is the nearest true discrete charge.

### Other repos, dreamed here

- **mw-collaib-triad.** A DAG catches propagation, never origination: it would have flagged the edot incident's four downstream items and not the sign error. collAIb is dormant 43 days; toesnail's hook runs a constant probe, 158 notes all pending.
- **inflownistration.** The 2017 thesis is an arity error: "processes information" is three-place and the sentence supplies one argument; the 2026 grammar's T is the missing decoder. Friston's FEP repeats the failure.
- **essay-wing.** The archive is one evening; the concept is thin and the lineage to .mw runs through a task-list parenthesis about annotations rotting. The essay wing is a genus (descriptions failing their subject), not a quantity.
- **zelegator-helferli.** Ten tools cap the router at 3.32 bits and the 50-case eval at 100 % carries all of them: the instrument is saturated. Silicon clears; data binds. "helferli is zelegator-compatible" has no artifact. The edge's energy case is the radio.
- **loderite-lodelore.** Cubic-26 beats BCC-14 on weighted path stretch (1.128 vs 1.239); BCC-14 buys real faces. Greedy meshing is void on whole cells, contra ARCHITECTURE.md:76. The ff32 salvage gate is unmet.
- **resogram-library.** The resogram is exactly |CWT|^2 for a causal order-1 gammatone; the owner's contributions are the route and the ebar kernel. Causality is its win, Lorentzian skirts its cost. Resogram.md:118's plus sign leaves a ripple the minus sign cancels exactly.

### Speculation (owner-authorised, results required sound)

- **why-three-plus-one.** V_eff''(r_0) = (4 - d) L^2/(m r_0^4), coupling-free, so stable orbits exist iff d < 4, classically and quantum-mechanically. Every argument varies d while holding the laws' form fixed. The ladder adds nothing: a method that works in every dimension cannot select one.
- **narrativium-formalized.** The simplicity prior inverts the myth: a million-to-one chance costs 19 to 20 bits, and a story exists for at most 2^{-c} of histories. Narrativium as dynamics is falsified in practice; as a fact about observers it is Pratchett's own sentence.

## Carry-back list

Only where the distilled version is sharper than the essay's own, not merely shorter.

1. **`lasercool.md` §4 and §9:** the two-threshold sentence becomes "A laser cannot be the exhaust: the brightness that makes it a laser is what empties it of entropy."
2. **`logic-layered-core.md` §0 box and §4.4 rule:** "It is the safety property" becomes "A core that cannot name a proof is a dashboard, not a supervisor; its safety is bought by demotion." [REFUSED: not applied, and correctly so. That sentence is a MERGE of two essays, as this file's own ladder entry says ("logic-layered-core §4.4, with counterfactual-boundary §6.4"), but the item names only one target. `logic-layered-core.md` calls the kernel a supervisor eight times (l.176, l.603, l.648, l.990) and argues that inexpressiveness is what lets it safely REMAIN one; "dashboard, not a supervisor" is `logic-counterfactual-boundary.md` l.572, reached by a stronger prior-stage argument (a core that cannot interpret arithmetic cannot represent a proof-as-sequence at all). Planting it in the parent alone asserts a conclusion that essay's text contradicts. A merged distillation needs both targets named, or a per-essay formulation each.]
3. **`zkml-proofs.md` §1:** "the zero-knowledge property over the weights is precisely what costs you the identity guarantee" becomes "You can prove you ran your model only by publishing it."
4. **`logic-scheduler-prototype.md` item 1:** "a purchase rather than a distinction" becomes "Reading (ii) is reading (i) plus a purchase, and at any plausible price for forcing the purchase loses."
5. **`logic-models-vs-epistemic.md` §2.3 and item 2:** the two-report sentence becomes "A theory determines independence; only a search has ignorance."
6. **`wirohsh-discontinuities.md` §7.2**, the replacement offered for wirohsh.md:82, becomes "The line is analytic versus not, and a smooth bump is on the shock's side; non-analytic data returns as a boundary value, at the price of a common annulus."
7. **`fhe-encrypted-algorithm.md` §3:** "always an encrypted circuit of fixed size" becomes "An encrypted program cannot branch on its data, so it is a circuit, and a general program has no worst case to pad to."
8. **`wirohsh-ladder.md` §5, verdict 3** (three paragraphs): becomes "Each rung hands back the continuous parameter the rotation was meant to remove; the payoff belongs to the Fourier relabelling, not to the descent." [REFUSED: not applied. The one-liner drops three things §5 establishes: the degrees-of-freedom counting proof that grounds the verdict (l.239, "every rung is overdetermined by exactly one variable's worth"), the locality obstruction as a SECOND and separate cost (l.253), and the explicit radial-case exception (l.261) where a rung does pay. §5 is a QUALIFIED structural finding, "not as a general descent, but yes when symmetry collapses the direction integral"; the compression makes it unqualified. This is the list's one distillation error, caught at carry-back rather than propagated. A shorter form that keeps the exception has not been found.]
9. **`spine.md` §1.5** and the README row: becomes "Before l.126 the maths arrives before the question; after it the questions arrive and no maths does."
10. **`acoustics.md` §0, third finding:** becomes "Sound can bend without echoing; light cannot, because Z = Z_0/n ties reflection to refraction."

Not carried back, already at their fixed point: omniscience, complementarity, proof-gauge, narrativium, simplex, mw-collaib.
