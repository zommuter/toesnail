# 2026-06-15 — Deliverable #1: the Resogram verify-marker pilot (scope + contract)

**Started:** 2026-06-15 14:09
**Session:** 26644694-49a1-48bc-bea7-1c0aae0f1b62
**Attendees:** 🏗️ Archie (architect), 😈 Riku (devil's advocate), ✂️ Petra (productivity), 🔬 Lennart (formal-methods / verification-tier lens), 🗄️ Cassi (derived-data / build-cache, content-hash cache keys)
**Topic:** Scope and contract the first real exercise of the `verify:` machinery — equation handles + inline markers + actual SymPy/Lean checks on `physics/Resogram.md`.

## Surfaced discoveries
(EMBED_ENDPOINT unset — `discoveries.md` not semantically retrieved; none surfaced.)

## Agenda
1. What "real SymPy/Lean checks" means — harness-now vs markers-first; does Resogram even have a Lean target?
2. Where verification output lives, given "source stays plain."
3. Equation-handle granularity + the macro mechanism.
4. Definition of done, given "AI emits findings, never edits the theory."

## Discussion

### Agenda 1 — what "real checks" means, and the Lean question
Archie anchored in `docs/rigor-debt.md:17-28`: all five Resogram candidates are `verify:sympy`/`verify:numeric`; **Resogram has zero `verify:lean` targets** (those live in the `toesnail.md` spine). The energy derivation is pure algebra — SymPy's job. Lennart: standing up Lean4+Mathlib for Resogram would discharge nothing; save it for Cauchy–Schwarz in the spine. Petra invoked N=2 — Resogram gives the Lean harness no consumer. Riku pushed the other half: markers-first proves *nothing*; the point of "pilot #1" is to validate the end-to-end loop, and the inventory already flags the `ė`-chain as a candidate ✗ — the best reason to **run it now**, since a pilot that can't survive a real error isn't a pilot.

**Owner override:** kept Lean in scope and asked "you can also use Lean for the SymPy claims, right?" — reframing the tier tag.

### Agenda 1b — does Lean subsume the SymPy claims?
Lennart: yes — tiers partition by **assurance strength × cost, not claim type**. SymPy = fast heuristic CAS verdict; Lean = kernel-checked certainty. The 3 pure-algebra claims are `ring`/`field_simp` near-one-liners in *both* tiers; `c=0` is medium; the ODE/convolution back-substitution is a multi-day formalization (differentiation under the integral sign) in either. Archie: this makes the tier a **floor, not a label** — toesnail owns that schema call. Riku: proving one cheap algebra claim in Lean *as well* gives the Lean harness its first real Resogram consumer (answers Petra's N=2), while refusing to commit the session to the expensive proof.

### Agenda 2 — where verification output lives
`CONVENTIONS.md:42-45` forbids pasting computed results inline. Lennart drew the key distinction: the **check** (`*.py`, `*.lean`) is *source* — version-controlled, re-runnable, the `.mw` "verification fragment" in embryo; the **verdict** (`✓`, value of `c`) is *cache* — disposable, regenerated by re-running. Riku: but the contract says the AI *emits findings* — a bare `verify/` dir makes the owner run code to discover a problem; the ✗ located-discrepancy narrative needs a written home. Reconciled: instruments in `verify/`, findings annotate `docs/rigor-debt.md` (closing the triage-menu loop), `Resogram.md` gains only markers + handles.

### Agenda 2b — the `verified:` attestation marker (owner extension)
Owner proposed an inline attestation ("verified by lean in proof 4acde4d") carrying a hash so the tool detects whether the claim drifted. Cassi (build-cache lens): hash a **canonical normal form**, not raw LaTeX, or cosmetic edits false-trigger — use SymPy `srepr` of the sympified claim. Archie: bind a **composite key** — claim-hash × instrument-file-hash (Nix-derivation pattern) — so either the claim *or* the proof changing invalidates. Riku fenced the §3 boundary: the attestation is a **signature/provenance**, not a result — tier + pointer + hashes are allowed inline; the actual value (`c=0`, `✓`) stays forbidden. Lennart spelled the lifecycle: `verify:` (open debt) → `verified:` (attested) → auto-stale on hash mismatch. Petra (N=2): write the *format* now; defer the automated staleness *checker* until acoustics is the 2nd consumer — but make the grammar machine-parseable from day one.

### Agenda 3 — handle granularity + the macro mechanism
**Finding:** the site renders with **MathJax 3** (`_includes/custom-head.html:1-3`), not KaTeX, and **no handle macros exist anywhere** — `CONVENTIONS.md:12` ("we already do this with the KaTeX macros defined at the top of README.md") is false on both counts. The pilot therefore establishes the mechanism for the first time, in MathJax (`tex: { tags: 'ams', macros: {…} }`). Lennart tied granularity to the work: **handle-where-marked** — each `verify:`-marked claim needs a stable handle to bind the attestation `claim=` hash; unmarked equations aren't tagged on spec. Riku: the convention doc is itself a *finding*, but a KaTeX→MathJax fix is tooling (the AI's lane), not theory.

### Agenda 4 — definition of done
Riku held the line: **done ≠ all-green.** If SymPy/Lean confirms the predicted `ė`-chain `−2βω²x²` residual, the AI **surfaces the located discrepancy and does not edit `Resogram.md`'s math**. Success = the loop ran (marker → instrument → verdict → finding → owner), whatever the verdict. Archie estimated Lean toolchain bring-up as the ~1–2-session long pole; sequence the cheap SymPy + markers + handles + conventions half first, the Lean proof last.

## Decisions

- **D1 — Dual-tier, run now.** SymPy on all five Resogram claims this session + Lean on the cheap algebraic subset (≥ the `ė` first-line identity); `c=0` attempted numerically. ODE/convolution back-substitution = deferred `verify:lean` debt. *Out of scope:* the Lean spine (`toesnail.md`), acoustics (pilot #2).
- **D2 — Tier = assurance floor.** `verify:<tier>` = "discharge with **at least** this tier," escalatable `sympy → lean`; not a claim-type label. Update `CONVENTIONS.md §2`. *Out of scope:* renaming/re-partitioning the three tiers.
- **D3 — Instruments are source, findings annotate rigor-debt.** `verify/` holds committed, re-runnable scripts/proofs. Outcomes annotate `docs/rigor-debt.md` per claim (pointer + ✗ note). `Resogram.md` gains only markers + handles. *Out of scope:* a separate findings log.
- **D4 — `verified:` attestation marker.** `<!-- verified:<tier> claim=<srepr-hash8> by=<instrument>@<filehash8> -->`. Composite hash (SymPy `srepr` canonical claim × instrument file hash). Inline = signature/provenance only; result forbidden (§3 carve-out). Lifecycle `verify:`→`verified:`, auto-stale on mismatch. Format now; automated checker deferred to acoustics (N=2). *Out of scope:* the source-walking checker this session.
- **D5 — Handles via MathJax, handle-where-marked.** MathJax config block in `custom-head.html`; handle each `verify:`-marked claim + existing cross-refs. Fix `CONVENTIONS.md §1` KaTeX→MathJax. *Out of scope:* unmarked equations, other docs.
- **D6 — Loop-ran = done; AI never edits the math.** Success = loop executes end-to-end whatever the verdict; AI surfaces ✗ as a finding, leaves the physics untouched. Lean proof sequenced last. *Out of scope:* all-green gating; AI-side discrepancy resolution.

## Action items

- [ ] Establish MathJax handle mechanism (`tex:{tags:'ams',macros}`) in `_includes/custom-head.html` + handles on the ~5 marked Resogram claims (`physics/Resogram.md`). Contract: `\eqref` cross-refs render; each marked claim has a stable content-meaningful id. <!-- id:96ad -->
- [ ] Author inline `verify:` markers (with instrument pointers) on the five Resogram claims (`physics/Resogram.md`). Contract: `grep -rn 'verify:' physics/Resogram.md` lists five tier-tagged markers. <!-- id:d0bf -->
- [ ] Write `verify/` SymPy scripts (one per claim) + `verify/README.md`; run all five, capture ✓/✗/inconclusive (`verify/resogram_*.py`). Contract: each `uv run`-able, prints a verdict, no output committed as ground truth. <!-- id:9b2d -->
- [ ] Write ≥1 Lean proof (the `ė` first-line identity); stand up Lean4+Mathlib via `lake` (`verify/Resogram.lean` + lakefile). Contract: `lake build` discharges the identity. *Heaviest item; may be its own session.* <!-- id:3317 -->
- [ ] Compute composite hashes + write `verified:` attestation markers for discharged claims (`physics/Resogram.md`). Contract: each parses as `verified:<tier> claim=<h8> by=<inst>@<h8>`; re-hashing unchanged claim/instrument matches. <!-- id:ee36 -->
- [ ] Annotate `docs/rigor-debt.md` with per-claim outcomes (incl. ✗ located-discrepancy narrative). Contract: each Resogram item shows outcome + instrument pointer. <!-- id:7340 -->
- [ ] Update `CONVENTIONS.md` — §1 KaTeX→MathJax + "already"→"as of this pilot"; §2 tier-as-floor; new attestation-grammar section + §3 signature-allowed/result-forbidden carve-out. <!-- id:3f57 -->
- [ ] (Deferred, GATED) Automated `verified:`/`verify:` staleness checker (walks source, recomputes hashes, flags drift). Gate: acoustics pilot #2 supplies the N=2 second consumer. <!-- id:04bb -->
